setup

```
npm i
```

dev
```
npm run dev
```

build
```
npm run build
```

publish
```
npm run publish
```



توضیح
در این پروژه، دیپلوی شدن صرفاً با git push کردن روی برنچ اصلی اتفاق نمی‌افتد.
این پروژه از پکیج npm به نام gh-pages برای دیپلوی استفاده می‌کند.
برای اینکه سایت روی گیت‌هاب به‌روز شود، باید دستور npm run publish را اجرا کنید.